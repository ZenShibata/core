export enum RabbitConstants {
    EXCHANGE = "nezu-gateway",
    QUEUE_RECV = "nezu-gateway.recv",
    QUEUE_SEND = "nezu-gateway.send",
    TASKS_SEND = "scheduled-tasks.send",
    TASKS_RECV = "scheduled-tasks.recv"
}
